{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main DWG audit desktop window.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "event:default",
    "dialog:allow-open"
  ]
}
