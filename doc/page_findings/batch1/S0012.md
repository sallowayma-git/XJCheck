# S0012 / 12 测控2开入回路图1.dwg

- page_type: `二次原理图（网格化很强的测控开入回路首页，主体是三列重复横向回路，顶部带电源/公共线引导带）`
- confidence: `0.90`
- structure_summary: `页面标题为 3-21n BINARY INPUT 1，与文件名“测控2开入回路图1”一致，属于测控开入回路第一页。版式上右下是标准标题栏，审计主区可分成三层：1) 顶部特殊引导带 y≈245-265，包含 3-21GD1/20/21、3-21KLP1、DC+/DC- 以及 231/232/331/332/532 这类行内数字；2) 主体重复回路带 y≈85-235，按 x 方向稳定分成三列，约为左列 x≈32.5-145、中列 x≈197.5-270、右列 x≈322.5-395，每一行都由短水平导线 + 小块符号 + 说明文字组成；3) 底部 y≈77.5 的三条短横线更像列头/分隔线，不像有效端子配对对象。统计上本页有 547 条 line、179 条 polyline、163 个 block、95 个 line_group，且 95 个 line_group 全部为 horizontal。已明确理解的是 y≈85-235 的重复开入回路区，以及右下标题栏；仍未完全理解的是顶部 y≈245-265 特殊引导带内的数字语义，以及各 PWF 块型各自对应的器件含义。`
- recognition_strategy: `当前应继续走 WireDiagramExtractor，不应切成纯 TableExtractor。虽然页面有明显“行列感”，但 table_like_geometry=False，而且真正稳定的主证据是 95 条水平 line_group，不是封闭表格单元。更合适的是在 WireDiagramExtractor 上增加 binary-input-grid / row-banded 模式：先按 y 轴识别重复行带，再在每一行带内按固定列锚点做单侧端子号提取，并对首页顶部特殊引导带单独分流。`
- number_matching_strategy: `本页数字匹配应主要依赖导线端点附近的自由文本数字，而不是表格单元，也不是块内文本。高置信数字几乎全部落在稳定文本列 x≈90.6、215.6/215.9、340.6/340.9，对应稳定端点列 x≈92.5/105、217.5/230(.25)、342.5/355(.25)。需要特别注意：很多编号会在同一小符号两侧各出现一次单侧命中，例如 112 同时出现在 G0496 和 G0497，128 同时出现在 G0498 和 G0499，213 同时出现在 G0500 和 G0501，因此应以“同 y、同数字、跨短块分裂的相邻 line_group 合并/去重”为主规则。顶部 y≈245 的 231/232/331/332/532 则不是端点贴靠型数字，需作为次级规则按行内 tap / inline numeric 处理。`
- high_confidence_signals: `1) 标题“3-21n BINARY INPUT 1”和文件名“测控2开入回路图1”一致指向开入回路二次原理图；2) route_target 现为 WireDiagramExtractor，audit_role=primary；3) 95 个 line_group 全为 horizontal，主结构非常稳定；4) 89 个 accepted terminal candidates 明显聚集在 5 个固定数字列，说明主体重复回路区已被很好地定位；5) y≈85-235 的大部分行已经能稳定抽到 112-127、128-131/201-212、213-228 这几组编号，只是当前被拆成左右两个单侧记录。`
- failure_modes: `1) 当前最大失败模式是把“同一编号跨小符号分成左右两段”的结构拆成两条记录，导致同号重复、无法形成高置信配对；2) 顶部首页特有的 G0502-G0504 长线/母线型结构不适合纯端点窗口，231/232/331/332/532 这类行内数字会被完全漏掉；3) y≈77.5 的 G0410-G0412 更像列头分隔线，如果继续送入常规配对会制造无意义 discard；4) 左列说明文字如“就地/Local/检修状态”、中右列“开入xx/BI xx/Object ...”与数字在 y 上紧邻，若窗口过宽容易把说明文本误吸进端点候选；5) 块名数量多且重复度高，若提高块内文本权重，容易把器件代号或固定块属性误当端子号。`
- script_change_suggestions: `1) 新增页级标签：当标题命中“BINARY INPUT 1/开入回路图1”且存在三列重复水平回路时，标记为 binary_input_first_page；2) 在 WireDiagramExtractor 增加 row-band clustering，先按 y 聚成重复行，再在每行中按稳定 x 列做候选筛选；3) 对同 y 且同数字、并且两个 line_group 只被一个短块间隔开的情况做 merge/dedup，直接解决 112、128、213 这类“双侧各记一次”的问题；4) 增加列锚点先验，优先保留 text_x 落在 90.6、215.6-215.9、340.6-340.9 附近的数字，并将 endpoint_x 限定在 92.5/105、217.5/230(.25)、342.5/355(.25) 这些稳定窗口；5) 将 y≈77.5 的列头短线和 y≈245-265 的首页引导带从常规 terminal pairing 中分流，前者直接降权或排除，后者走 header-bus / inline-numeric 专用逻辑；6) 对本页型继续压低 block-internal numeric 权重，优先使用 DIM 层自由文本数字；7) 在审计结果里增加 duplicate_single_sided_hits 和 inline_numeric_on_bus 两类原因，便于后续批量复核。`
