# S0021 / 21 左侧端子图1.dwg

page_type: `屏端子图 / 左侧端子图（LEFT TERMINAL 1，supplemental）`

confidence: `0.86`

structure_summary: 这页标题为 `LEFT TERMINAL 1`，文件名是 `左侧端子图1`，当前主链已将其归为 `屏端子图` 并走 `TerminalDiagramExtractor`。主审计区不是封闭表格，而是 4 组重复的横向端子列带：`x≈40-115` 共 33 行、`x≈127.5-202.5` 共 45 行、`x≈217.5-292.5` 共 46 行、`x≈310-385` 共 11 行；所有 135 个 line_group 都是 horizontal，y 方向基本按 5 单位步长排布到 `45-270`。数字锚点高度集中在 `x≈63.5/64.2`、`150.998/151.749`、`240.998`、`333.498`，相邻的端子代号/语义列集中在 `x≈71.0`、`158.5`、`248.5`、`311.0`、`341.0`。页内已经能稳定看到 `ZD / 3-21UD / 3-21ID / 3-21GD / 3-21QD` 等列带标题，以及 `上接3-21QD24/72`、`下接3-21QD25/73` 这类续接提示。当前真正被理解的，是那些“局部序号列 + 相邻端子代号列 + 水平短导线”构成的标准端子行；当前还没吃透的，是左半下段 `y≈185-270` 附近由 `DK/KLP/ZKK/UA/UB/UC/UN/3U0`、`+/-/&`、`未定义...回路图` 混在一起的语义区。

recognition_strategy: 当前应继续走 `TerminalDiagramExtractor`，但要从“宽松端点窗口”升级到“terminal-strip column aware”子模式，而不是改走 `TableExtractor`。证据很直接：`table_like_geometry=False`，块只有 3 个、polyline 只有 17 条，页面主结构不是表格格元，而是固定宽度的端子列带；同时 16 条 `terminal_strips` sidecar 元数据已经可用，页内文本也明确出现 `ZD / 3-21UD / 3-21ID / 3-21GD / 3-21QD`。更合理的流程是先按 4 个稳定 span 家族和 5 单位 y 步长聚成“端子行”，再在每个行内区分“局部序号列”“端子代号列”“说明/跨页引用列”，而不是让所有候选在一个大窗口里竞争。

number_matching_strategy: 这页数字匹配更依赖 `端子列带`，导线只负责提供行定位，`不是` 依赖表格。理由有三层：1) 数字候选明显不是落在表格格元，而是聚集在 6 个稳定 x 列上；2) 右值很多并不是独立数字，而是从 `3-21n211`、`3-21n318`、`1-21n109`、`3-21n330` 这类带前缀的端子代号里抽出来的；3) 当前 108 条 discard 的主因不是“没数字”，而是同一条水平线会同时贴住同列上下相邻的 3-4 个局部序号和 3-4 个端子代号，出现 `ambiguous candidate ordering`。因此本页的主策略应是“列带先验 + 行锁定 + 代号正则抽值”，而不是单纯的导线端点最近邻，更不是表格单元 OCR。

main_chain_readiness: 这页端子图已经能进主链，不是因为它已经有高置信 pair，而是因为它已经满足“稳定归类、稳定抽取、低风险挂载”三个条件。第一，页型判断已经稳定落在 `屏端子图`，`audit_role=supplemental`、`route_target=TerminalDiagramExtractor`，不会把端子页误当主原理图去压审计结论。第二，实跑产物不是空壳：本页有 `433` 条文本、`135` 个 line_group、`1494` 个 terminal candidate、`877` 个 pair candidate、`135` 个带证据 pair，说明抽取链已能完整穿透页面。第三，27 条 review 全部集中在左半下段已知难区，另外两组右侧列带没有 review 爆炸，当前接入主链只会提供补充证据，不会像错误 primary 路由那样污染主审计结果。

high_confidence_signals:
1. 标题 `LEFT TERMINAL 1`、文件名 `左侧端子图1`、`.prj` 页序和 `terminal_xml` 侧车信息一致，把本页稳定落到左侧端子图页型。
2. 主页面结构非常规整：`135` 个 line_group 全是 horizontal，并稳定收敛到 4 个 span 家族：`40-115`、`127.5-202.5`、`217.5-292.5`、`310-385`。
3. 865 个 accepted terminal candidate 明显集中在固定列锚点：左侧局部序号列约 `63.5/64.2`、`150.998/151.749`、`240.998`、`333.498`；相邻端子代号列约 `71.0`、`158.5`、`248.5`、`311.0`、`341.0`。
4. sidecar 已提供 16 条 terminal strip 元数据，且本页正文直接命中 `ZD / 3-21UD / 3-21ID / 3-21GD / 3-21QD` 与 `上接/下接` 标记，说明这不是“只有线、没有 strip 语义”的盲页。
5. 27 条 review pair 全部落在 `x≈40-115` 与 `127.5-202.5` 的下段 `y≈185-270`，失败区域集中且可解释，没有出现全页随机失控。

failure_modes:
1. 最大失败模式是 `ambiguous candidate ordering`：例如 `127.5->202.5` 这一列带里，`y=45` 的一条线同时贴住左侧 `20/21/22/23` 与右侧 `210/211/212/213`，最终形成 `21->211`、`21->212`、`22->211` 等多组近似候选，导致 108 条 pair 被降为 discard。
2. 左半下段 `y≈185-270` 的 review，核心不是 OCR 漏数字，而是右侧邻列变成了 `3-21DK2-2`、`3-21KLP1-1`、`3-21ZKK-1`、`UA/UB/UC/UN/3U0`、`+/-/&` 这类语义标签，当前规则不知道这些是否应映射到某种“端子语义右值”，于是统一落成 `missing right candidate`。
3. `未定义...回路图` 这类跨页引用注释在 `x≈70 / 157.5 / 247.5 / 340` 一带大量出现，y 又紧贴有效行，当前如果只看端点邻近关系，很容易把它们和端子代号列混在一起。
4. `说明`、`上接3-21QD24/72`、`下接3-21QD25/73`、strip 标题这些表头/续接文字与有效行共用相同列系，若不单独分流，会继续制造假候选或拉宽行窗口。
5. 最右侧 `310->385` 的 11 行虽然表面上都有数字，但左值有时来自 `1-21n109/110`，右值来自 `3-21n322-330`，局部序号 `73-81` 也在同一区域共存；这一区当前能出值，但还缺一层“哪一列才是真正 pair 左右端”的明确约束。

unresolved_areas:
1. 左半下段 `y≈185-270` 的 `DK/KLP/ZKK` 区，到底应该输出“局部序号 -> 语义设备端”还是直接视为非 pair 语义行，目前没有页型级规则。
2. `UA/UB/UC/UN/3U0` 这些量测/相别标签与右侧 `3-21n720/721/...` 是否属于同一条端子链，当前只看到局部样本，还没形成稳定归纳。
3. `未定义...回路图` 文本更像跨页来源提示，但它究竟应该沉为备注、还是参与后续跨页追踪，目前仍未定。
4. 右上 `310->385` 短列带更像续接桥接区，当前规则能抽出数字，但还没完全确认应以 `1-21n109/110` 为左列，还是以 `73-81` 为左列。

script_change_suggestions:
1. 在 `TerminalDiagramExtractor` 中增加 `left_terminal_strip_grid` / `terminal_strip_column_mode`：先按 span 家族 `40-115`、`127.5-202.5`、`217.5-292.5`、`310-385` 分组，再按 5 单位 y 步长锁行。
2. 把候选窗口拆成列角色，而不是统一邻近窗：`40->115` 优先看 `x≈63.5-64.3` 的局部序号与 `x≈71.0` 的代号列；`127.5->202.5` 优先看 `x≈150.9-151.8` 与 `x≈158.5`；`217.5->292.5` 优先看 `x≈240.9-241.1` 与 `x≈248.5`；`310->385` 需单独区分 `x≈311.0`、`333.5`、`341.0` 三列角色。
3. 对 `\d+-\d+n(\d+)` 这类端子代号建立高优先级正则抽值，并把 `DK/KLP/ZKK`、`UA/UB/UC/UN/3U0`、`+/-/&` 显式归到语义列，避免继续与数值列同池竞争。
4. 对 `未定义.*回路图`、`说明`、`上接/下接`、strip 标题建立页型级黑名单或旁路通道，不让它们进入常规 numeric ranking。
5. 对左半下段当前 27 条 review，建议先增加一个 `semantic_terminal_row` 分支：若左值存在、右侧仅命中 `DK/KLP/ZKK/相别` 文本，则先输出“语义右端待解释”而不是笼统 `missing right candidate`。
6. 对最右侧 `310->385` 的短列带增加专用窗口试验：分别测试 `311->341` 与 `333.5->341` 两种左右列解释，并把结果记录为候选模式，避免把续接桥接区和标准端子行混为一谈。

candidate_window_suggestions:
1. 行锁定窗口建议优先用 `|text_y - line_y| <= 1.2`，超过这个范围的相邻行数字只保留为备选，不直接参与 selected pair。
2. `40->115` 列带建议用 `left_local_x=63.3-64.4`、`right_code_x=70.0-72.2`、`aux_semantic_x=88.0-91.5`。
3. `127.5->202.5` 列带建议用 `left_local_x=150.8-151.9`、`right_code_x=157.8-159.2`，并屏蔽 `157.49` 一线的 `未定义...` 注释。
4. `217.5->292.5` 列带建议用 `left_local_x=240.8-241.2`、`right_code_x=247.8-249.2`，并屏蔽 `247.49` 一线的跨页引用注释。
5. `310->385` 列带建议把 `311.0`、`333.5`、`341.0` 三列分开做 A/B 测试，不要继续复用前三组列带的统一端点窗口。
